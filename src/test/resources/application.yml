spring:
  sendgrid:
    api-key: api-key

services:
  internal:
    username: user
    password: password

  crm:
    name: crm-service
    url: https://crm-service.com
    username: user
    password: password